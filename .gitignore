node_modules
build
npm-debug.log
.env
.env.local

# The local libSQL file used when developing without a Turso account.
.data/
.DS_Store

# A one-off script left in the test folder while measuring something;
# the suite's glob already ignores the name, and so does git.
test/_*.tmp.mjs

# Capture intermediates — the media in docs/media is what ships.
tools/capture/out/
tools/capture/voice
*.webm

# Synthesised narration, keyed by what was said. Rebuilt on demand and
# large; the mp4s it ends up inside are what ship.
tools/capture/voice/

# Artefacts of old agent tooling, never part of the app.
.claude-flow/
.hive-mind/
